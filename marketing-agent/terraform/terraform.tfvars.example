# Copy to terraform.tfvars and fill in — OR pass with -var on the CLI.
# terraform.tfvars is gitignored; never commit real secret values.

# --- Required secrets (no defaults) ---
gemini_api_key     = "AIza...your-gemini-key..."
gmail_app_password = "abcd efgh ijkl mnop"   # 16-char Gmail App Password

# --- Optional overrides (sensible defaults already set in variables.tf) ---
# project_id             = "nohungrypets"
# region                 = "europe-west1"
# scheduler_time_zone    = "Europe/London"
# scheduler_cron         = "0 9 * * 1,3,5"
# schedule_start_date    = "2026-08-04"
# launch_countdown_until = "2026-08-18"   # set ~2 weeks out at launch, then remove
# recipient_email        = "agipasieka79@gmail.com"
# gmail_sender           = "agipasieka79@gmail.com"
